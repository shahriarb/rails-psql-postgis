== Basebones Postgis/Rails sample

This application sets out to test the Cloud66 support for Postgis. 

The application has a single Location model, and include 3 seed data records. The root path should
display a table of these locations. 
